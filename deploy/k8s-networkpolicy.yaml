apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: restrict-inference
spec:
  podSelector:
    matchLabels:
      app: inference
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: default
